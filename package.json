{
  "name": "@safe/workspace",
  "private": true,
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
